# Runtime dependencies for the Chicken Nugget Problem Solver.
# Streamlit Community Cloud installs exactly this file.
# Streamlit is the only direct dependency; it pulls in Pillow (used to serve
# the images in assets/) on its own.
streamlit>=1.40
